{
  "name": "orchestrator",
  "private": true,
  "scripts": {
    "build": "bun build src/handler.ts --outfile dist/index.js --target node --format cjs --external '@aws-sdk/*'",
    "zip":"bun run build && Compress-Archive -Force -Path dist/index.js -DestinationPath dist/lambda.zip",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.5.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@aws-sdk/client-ecs": "^3.1020.0",
    "@stream-ops/logger": "workspace:*"
  }
}
